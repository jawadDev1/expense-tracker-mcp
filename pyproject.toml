[project]
name = "expense-tracker-mcp"
version = "0.1.0"
description = "Expense Tracker MCP server"
requires-python = ">=3.14"
dependencies = [
    "fastmcp>=3.4.5,<4.0",
    "pyjwt>=2.13.0",
]

[tool.uv]
package = false
